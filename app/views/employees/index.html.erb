<div class="row">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Department</th>
      <th>Branch</th>
    </tr>
  </thead>
  <tbody>
    <% @employees.each do |employee| %>
    <tr>
      <td><%= employee.first_name+" "+employee.last_name %></td>
      <td><%= employee.email_address %></td>
      <td><%= employee.phone_number %></td>
      <td><%= Department.find_by(id: employee.department_id).department_name %></td>
      <td><%= Branch.find_by(id: employee.branch_id).city %></td>
    </tr>
    <% end %>
  </tbody>
</table
</div>
